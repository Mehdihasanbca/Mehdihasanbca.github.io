(()=>{const list=document.querySelector('[data-jobs-list]');if(!list)return;const search=document.querySelector('[data-job-search]');const country=document.querySelector('[data-job-country]');const category=document.querySelector('[data-job-category]');const count=document.querySelector('[data-job-count]');const empty=document.querySelector('[data-jobs-empty]');let jobs=[];const form='https://forms.gle/1nVwzXHiHV9Cjw4A9';const channel='https://whatsapp.com/channel/0029Vb7mJuWF1YlQJ0sKSn06';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const render=()=>{const q=(search?.value||'').trim().toLowerCase();const c=country?.value||'';const cat=category?.value||'';const filtered=jobs.filter(j=>(!q||[j.title,j.country,j.city,j.category,j.summary].filter(Boolean).join(' ').toLowerCase().includes(q))&&(!c||j.country===c)&&(!cat||j.category===cat));if(count)count.textContent=String(filtered.length);list.innerHTML='';if(!filtered.length){if(empty)empty.hidden=false;return}if(empty)empty.hidden=true;filtered.forEach(j=>{const article=document.createElement('article');article.className='job-card';article.innerHTML=`<div class="job-card-top"><div><h3>${esc(j.title)}</h3><p>${esc(j.summary||'Verified AVC vacancy')}</p></div><span class="job-chip active">${esc(j.status||'Open')}</span></div><div class="job-meta">${j.country?`<span class="job-chip">${esc(j.country)}</span>`:''}${j.city?`<span class="job-chip">${esc(j.city)}</span>`:''}${j.category?`<span class="job-chip">${esc(j.category)}</span>`:''}${j.salary?`<span class="job-chip">${esc(j.salary)}</span>`:''}</div>${j.requirements?`<p><strong>Key requirements:</strong> ${esc(j.requirements)}</p>`:''}${j.deadline?`<p><strong>Deadline:</strong> ${esc(j.deadline)}</p>`:''}<div class="job-actions"><a class="button primary" href="${form}" target="_blank" rel="noopener noreferrer">Apply now</a><a class="button outline" href="${channel}" target="_blank" rel="noopener noreferrer">Official updates</a></div>`;list.appendChild(article)})};const buildFilters=()=>{const countries=[...new Set(jobs.map(j=>j.country).filter(Boolean))].sort();const cats=[...new Set(jobs.map(j=>j.category).filter(Boolean))].sort();countries.forEach(v=>country?.insertAdjacentHTML('beforeend',`<option value="${esc(v)}">${esc(v)}</option>`));cats.forEach(v=>category?.insertAdjacentHTML('beforeend',`<option value="${esc(v)}">${esc(v)}</option>`))};fetch('data/jobs.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error('jobs data unavailable'))).then(data=>{jobs=Array.isArray(data.jobs)?data.jobs:[];buildFilters();render()}).catch(()=>{jobs=[];render()});[search,country,category].forEach(el=>el&&el.addEventListener(el===search?'input':'change',render))})();